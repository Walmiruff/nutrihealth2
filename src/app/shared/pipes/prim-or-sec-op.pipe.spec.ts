import { PrimOrSecOpPipe } from './prim-or-sec-op.pipe';

describe('PrimOrSecOpPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimOrSecOpPipe();
    expect(pipe).toBeTruthy();
  });
});
