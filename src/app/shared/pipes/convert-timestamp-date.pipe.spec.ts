import { ConvertTimestampDatePipe } from './convert-timestamp-date.pipe';

describe('ConvertTimestampDatePipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertTimestampDatePipe();
    expect(pipe).toBeTruthy();
  });
});
